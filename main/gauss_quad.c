#include <stdio.h>
#include <math.h>
#include <time.h>
#include <stdlib.h>



	double Gauss ( double min , double max , double (*f) (double )){
		
		
		
		}



int main (){
	
	/*
	 * Vettore zeri per il polinomio di legendre di grado 5.
	 */
	
	double zeri[5] = {0,
					  -sqrt((35-2*sqrt(70))/7)/3,
					  +sqrt((35-2*sqrt(70))/7)/3,
					  -sqrt((35+2*sqrt(70))/7)/3 ,
					  +sqrt((35+2*sqrt(70))/7)/3  };
					  
					  
					  
					  
	}
