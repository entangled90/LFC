#include "random.h"
#include <stdio.h>

int main () {
  int i = 0;
  double x*;
  x = malloc( 10000*sizeof(double));
  root_exp_dble(x,10000);
  FILE *f = fopen ("temp.dat","w");
  for ( i = 0; i< 10000; i++){
    
  }
  
}